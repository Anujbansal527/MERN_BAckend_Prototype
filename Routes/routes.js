const express = require("express");
const router = express.Router();

//importing controller for linking path with process
const { /* controllers name */} =  require( /* path of the controller */);

//router for performing task on api put,push,patch,create,delete

router.post(/* "/address" */" ", /* controller name */);
router.all;

//export for useable for other
module.export = router;


